"""Optional, deferred (docs/DECISIONS.md D2 phase 2): imports a small real
road-network extract (via OpenStreetMap / osmnx) for a "real city" demo, once
the algorithm is validated on the synthetic grid from generate_grid.py.

Not started yet — do not begin until BUILD-PLAN.md Steps 1-7 are complete on
the synthetic grid.
"""
